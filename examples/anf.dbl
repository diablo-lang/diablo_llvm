x_global = 1;

fn z(x: int, y: int) -> int {
    z = if (x > y) {
        x - y
    } else {
        y - x
    };
    z + x_global
}

fn add(a: int, b: int) -> int {
    c = a + b;
    return c
}

fn foo(x: int, y:  int) -> int {
    z = add(x, y);
    z + 1
}

x = foo(12, 13) + z(14, 15);
