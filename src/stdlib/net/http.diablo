module Http {
    fn xcreate_socket() -> int {
        extern create_socket()
    }

    fn xbind_socket(sockfd: int) -> int {
        extern bind_socket(sockfd)
    }

    fn xlisten_socket(sockfd: int) -> int {
        extern listen_socket(sockfd)
    }
}
