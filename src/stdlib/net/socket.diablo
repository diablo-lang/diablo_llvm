fn xadd(int a, int b) -> int {
    extern ext_add(a, b)
}

fn socket(int domain, int type, int protocol) -> int {
    extern socket(domain, type, protocol)
}

fn listen(int sockfd, int backlog) -> int {
    extern listen(sockfd, backlog)
}

fn close(int fd) -> int {
    extern close(fd)
}

main() {
    1
}